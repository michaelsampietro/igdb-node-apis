export type VideoGame = {
  checksum: string;
  first_release_date: number;
  name: string;
  rating: number;
  slug: string;
  storyline: string;
  summary: string;
  total_rating: number;
  url: string;
  version_title: string;
  videos: number[];
  websites: number[];
}

// image url example https://images.igdb.com/igdb/image/upload/t_original/{image_id}.jpg